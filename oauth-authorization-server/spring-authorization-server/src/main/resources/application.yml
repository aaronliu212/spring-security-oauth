server:
  port: 9000
  servlet:
    context-path: /oauth/authorization
  forward-headers-strategy:
    NATIVE

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.springframework.security: INFO
    org.springframework.security.oauth2: INFO